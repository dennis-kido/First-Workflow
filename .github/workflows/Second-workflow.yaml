name: Second Workflow
on: [push]
  
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
           - name: Install npm
             run: npm ci

           - name: Check npm version
             run: |
                 npm -v
                 echo " npm version checked!"
                 node -v
